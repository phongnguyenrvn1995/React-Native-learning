import React from "react";
import { SafeAreaView, Text } from "react-native";

function createMyContainer(): any {
    return(<SafeAreaView style={{flex: 1, backgroundColor: 'red'}}>
    </SafeAreaView>)
}

export default createMyContainer